# Environment Configuration for MSP Fleet Dashboard

# Server Configuration
PORT=3000
NODE_ENV=production

# Authentication
# IMPORTANT: Change this to a secure password in production!
MASTER_PASSWORD=Smart@2026!

# Session Security
# Generate a random secret: openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here

# CORS Configuration
# In production, set this to your domain
# CORS_ORIGIN=https://your-domain.com
CORS_ORIGIN=*

# Database Path (for future use)
DATABASE_PATH=./data/pbx.db
